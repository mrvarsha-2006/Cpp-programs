// You are using GCC
#include<iostream>
int main()
{
    int a,b,c;
    std::cin>>a>>b>>c;
    if (a+b+c==180)
        if (a==60&&b==60&&c==60)
            std::cout<<"Prize 3";
        else if(a==90||b==90||c==90)
            std::cout<<"Prize 2";
        else
            std::cout<<"Prize 1";
    else
        std::cout<<"No prize";
}
